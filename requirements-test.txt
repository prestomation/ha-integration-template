pytest
pytest-cov
pytest-timeout
requests
websockets

# Linting / formatting (also enforced in CI by .github/workflows/lint.yml).
ruff

# The component tier (tests/component) runs against a REAL in-process Home
# Assistant via this harness. It pulls in pytest-socket, which BLOCKS real
# network access — so it must NOT be installed for the Docker integration tier
# (tests/integration), and the two run as separate pytest invocations. The pure
# unit tier (tests/unit) needs only `pip install pytest`.
pytest-homeassistant-custom-component

# Provides the ``hass_frontend`` module that the ``frontend`` dependency imports
# at setup. Without it the component tier can't set up the config entry (the
# integration depends on ``frontend`` for the panel + card). Docker tier doesn't
# need it (the real HA image ships the frontend).
home-assistant-frontend

