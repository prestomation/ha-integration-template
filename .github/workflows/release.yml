name: Release

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine release version
        id: check
        run: |
          VERSION=$(python3 -c "import json; print(json.load(open('custom_components/example_integration/manifest.json'))['version'])")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

          # Pre-release versions (e.g. 0.2.0b1) become GitHub pre-releases: HACS
          # only offers them to users who enabled "Show beta versions".
          if [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+(a|b|rc)[0-9]+$ ]]; then
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
          elif [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          else
            echo "::error::manifest.json version '$VERSION' is malformed — expected X.Y.Z or X.Y.Z{a|b|rc}N."
            exit 1
          fi

          if git rev-parse --verify "refs/tags/v$VERSION" >/dev/null 2>&1; then
            echo "Tag v$VERSION already exists — nothing to release."
            echo "release=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if ! grep -q "^## \[$VERSION\]" CHANGELOG.md; then
            echo "::error::manifest.json is at $VERSION but CHANGELOG.md has no '## [$VERSION]' section."
            exit 1
          fi

          PANEL_VERSION=$(grep -E '^PANEL_VERSION\s*=' custom_components/example_integration/const.py | sed -E 's/.*"([^"]+)".*/\1/')
          if [ "$PANEL_VERSION" != "$VERSION" ]; then
            echo "::error::manifest.json version ($VERSION) does not match const.py PANEL_VERSION ($PANEL_VERSION). Bump both in the same PR."
            exit 1
          fi

          echo "release=true" >> "$GITHUB_OUTPUT"
          echo "Will release v$VERSION"

      - name: Extract changelog section
        if: steps.check.outputs.release == 'true'
        id: changelog
        env:
          VERSION: ${{ steps.check.outputs.version }}
        run: |
          awk -v ver="$VERSION" '
            $0 ~ "^## \\[" ver "\\]" { found=1; print; next }
            found && /^## \[/ { exit }
            found { print }
          ' CHANGELOG.md > release_notes.txt
          if [ ! -s release_notes.txt ]; then
            echo "::error::Failed to extract changelog section for $VERSION."
            exit 1
          fi
          {
            echo 'notes<<RELEASE_NOTES_EOF'
            cat release_notes.txt
            echo 'RELEASE_NOTES_EOF'
          } >> "$GITHUB_OUTPUT"

      - uses: actions/setup-node@v4
        if: steps.check.outputs.release == 'true'
        with:
          node-version: "20"

      - name: Install frontend deps
        if: steps.check.outputs.release == 'true'
        run: npm ci --prefix custom_components/example_integration/frontend

      - name: Build panel JS
        if: steps.check.outputs.release == 'true'
        run: bash ci/build-panel.sh

      - name: Build release zip
        if: steps.check.outputs.release == 'true'
        run: bash ci/build-zip.sh

      - name: Verify release zip
        if: steps.check.outputs.release == 'true'
        run: |
          if [ ! -f example_integration.zip ]; then
            echo "::error::example_integration.zip not found"
            exit 1
          fi
          echo "example_integration.zip exists ($(du -h example_integration.zip | cut -f1))"

      - name: Create and push tag
        if: steps.check.outputs.release == 'true' && github.event_name != 'pull_request'
        env:
          VERSION: ${{ steps.check.outputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "v${VERSION}"
          git push origin "v${VERSION}"

      - name: Create GitHub Release
        if: steps.check.outputs.release == 'true' && github.event_name != 'pull_request'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "v${{ steps.check.outputs.version }}"
          name: "v${{ steps.check.outputs.version }}"
          body: ${{ steps.changelog.outputs.notes }}
          draft: false
          prerelease: ${{ steps.check.outputs.prerelease == 'true' }}
          files: example_integration.zip
